# ARKHE-χ SafeManifold

`arkhe-safe-manifold` projects dynamic system security states into a canonical
equivalence-class space.

v0.2.1 includes correct fixes for `neron_model_checked` and `compute_observer_defect`.
