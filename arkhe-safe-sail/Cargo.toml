[package]
name = "arkhe-safe-sail"
version = "0.1.0"
edition = "2021"

[dependencies]
kani = { version = "*", optional = true }

[features]
default = []
kani = ["dep:kani"]

[lints.rust]
unexpected_cfgs = { level = "allow", check-cfg = ['cfg(kani)'] }
